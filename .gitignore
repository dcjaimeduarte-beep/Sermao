node_modules
dist
dist-web
*.local
.DS_Store
.env
.env.*

# Editor configs pessoais (não commitar — tasks/extensions/launch ficam no repo)
.cursor/
.vscode/settings.json

# PDFs de referência — grandes demais para GitHub (use Git LFS se necessário)
docs/*.pdf

# Zip só na raiz (a cópia de upload fica em sermao-deploy/)
/sermao-deploy.zip

# Fontes brutas da Bíblia
public/bible/_source.json
public/bible/_ra.json
public/bible/_ot.json
public/bible/_nt.json

# Proxy com chave real — nunca commitar
proxy/openai.php
